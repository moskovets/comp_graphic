#ifndef ALGO_H
#define ALGO_H
#include <vector>
#include <point.h>
#include <qpoint.h>

using namespace std;

int SomeAlgo(vector<tPoint> &vec, const QPointF &start, const QPointF &end);

#endif // ALGO_H
